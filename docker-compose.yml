name: tally-up

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tallyup
      POSTGRES_PASSWORD: tallyup
      POSTGRES_DB: tallyup_test
    ports:
      - "5433:5432"
